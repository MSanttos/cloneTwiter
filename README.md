# cloneTwiter

# expemplo
//variáveis de paginação
    $total_registros_pagina = 5;
    $deslocamento = 0;
    $pagina = 1;

    //página 2
    $total_registros_pagina = 5; //limit
    $deslocamento = 5; //offset desconsiderando os 5 primeiros registros
    $pagina = 2;